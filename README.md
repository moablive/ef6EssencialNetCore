# ef6EssencialNetCore - Template Base

Este projeto serve como um template base para [descreva o tipo de aplicação, ex: APIs RESTful, aplicações web] utilizando [principais tecnologias, ex: .NET Core, Node.js]. Ele inclui configurações para autenticação JWT, migrations de banco de dados, e o padrão Repository.

## Configuração do Ambiente

### Pré-requisitos

* Docker e Docker Compose (para o ambiente de banco de dados)
* .NET SDK (especifique a versão, ex: .NET 6.0 ou superior)

### Banco de Dados (Dockerizado)

Este projeto utiliza um banco de dados [Especifique qual SGBD, ex: PostgreSQL, MySQL, SQL Server] rodando em um container Docker. A configuração do serviço do Docker está definida no arquivo `docker-compose.yml` (ou similar).

* **Nome da Conexão (Connection String Name):** `DefaultConnection`
    * Verifique o arquivo de configuração `appsettings.json` para detalhes completos da string de conexão. Ela geralmente apontará para o serviço Docker (ex: `Host=localhost;Port=XXXX` ou o nome do serviço Docker).

Para iniciar o container do banco de dados:
```bash
docker-compose up -d # Ou o comando específico para seu SGBD no Docker
Variáveis de Ambiente / Configuraçõesappsettings.json:author: Nome do autor da aplicação.ConnectionStrings:DefaultConnection: String de conexão principal para o banco de dados.Jwt:Key: Chave secreta para assinatura dos tokens JWT (MUITO IMPORTANTE: use um valor seguro e longo em produção e não o exponha publicamente).Jwt:Issuer: Emissor do token.Jwt:Audience: Audiência do token.Outras configurações podem ser adicionadas conforme necessário.Como Executar o ProjetoClone o repositório:git clone [URL_DO_SEU_REPOSITORIO_AQUI]
cd [NOME_DA_PASTA_DO_PROJETO]
Inicie o container do banco de dados (se ainda não estiver rodando):docker-compose up -d
Instale as dependências da aplicação .NET:dotnet restore
Aplique as migrações do banco de dados (Entity Framework Core Migrations):dotnet ef database update
(Certifique-se de ter o dotnet-ef tools instalado globalmente ou como local tool).Execute a aplicação:dotnet run
A API estará disponível em http://localhost:porta ou https://localhost:porta (verifique o output do console).Arquitetura e PadrõesAutenticação JWT (JSON Web Tokens): Utilizado para proteger endpoints da API.Migration Models (Entity Framework Core): O esquema do banco de dados é gerenciado através de migrações, permitindo versionamento e evolução da estrutura do banco.Padrão Repository e Unit of Work: A camada de acesso a dados é abstraída utilizando esses padrões, promovendo separação de responsabilidades, facilitando testes e gerenciamento de transações.AutoMapper: Utilizado para mapeamento entre objetos (ex: Entidades para DTOs).Injeção de Dependência: Amplamente utilizada para gerenciar as dependências entre os componentes da aplicação.CORS (Cross-Origin Resource Sharing): Configurado para permitir requisições de origens específicas (definido em PermitirApiRequest).Acesso e AutenticaçãoCredenciais de Login (Usuário Padrão/Teste)Para acessar o sistema (provavelmente através de um endpoint de login como /api/auth/login - verifique o controller de autenticação), utilize as seguintes credenciais:Usuário: guilhermeferrazbonato@gmail.comSenha: Guilherme@1998Atenção: Estas são credenciais de desenvolvimento/teste. Certifique-se de alterá-las ou usar um sistema de gerenciamento de usuários seguro em produção.Autenticação API (Bearer Token)Após efetuar o login na aplicação (via endpoint de login da API), você receberá um token de autenticação (JWT).Para acessar endpoints protegidos da API, você precisará incluir este token no cabeçalho Authorization das suas requisições, prefixado com Bearer .Exemplo de como usar o token (em uma ferramenta como Postman, Insomnia, ou via código):No cabeçalho (Header) da sua requisição HTTP:Authorization: Bearer SEU_TOKEN_JWT_AQUI
Exemplo com o token fornecido (este token é apenas um exemplo e provavelmente já expirou):Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1bmlxdWVfbmFtZSI6Imd1aWxoZXJtZWZlcnJhemJvbmF0b0BnbWFpbC5jb20iLCJWQUxPUl9BIjoiVkFMT1JfQiIsImp0aSI6ImEzODFjMDJmLWI2YmUtNGFkYi1iZTVkLTYwNGQxNzc5YWY0MyIsImV4cCI6MTcwNDcyNjM5MSwiaXNzIjoiR3VpbGhlcm1lX0lzc3VlciIsImF1ZCI6Ikd1aWxoZXJtZV9BdWRpZW5jZSJ9.9Mv9jvSlC7aL4cu4JYNZGkEE7YvzWrwBLja3j8beIDc
Endpoints da APITodos os endpoints abaixo requerem autenticação JWT (Bearer Token no header Authorization), exceto se indicado de outra forma.Categorias (/api/categorias)GET /api/categorias/authorDescrição: Retorna o nome do autor e a string de conexão configurados na aplicação (para fins de debug/informação).Resposta: stringGET /api/categorias/saudacao/{nome}Descrição: Retorna uma saudação personalizada utilizando um serviço injetado.Parâmetros de Rota:nome (string): Nome para a saudação.Resposta: stringGET /api/categorias/produtosDescrição: Lista todas as categorias que possuem produtos associados.Resposta: 200 OK com IEnumerable<CategoriaDTO> | 404 Not FoundGET /api/categoriasDescrição: Lista todas as categorias de forma paginada.Parâmetros de Query (para paginação, ex: CategoriaParameter): PageNumber, PageSize.Headers de Resposta: X-Pagination com metadados da paginação.Resposta: 200 OK com IEnumerable<CategoriaDTO> | 404 Not FoundGET /api/categorias/{id}Descrição: Obtém uma categoria específica pelo seu ID.Parâmetros de Rota:id (int): ID da categoria.Resposta: 200 OK com CategoriaDTO | 404 Not FoundPOST /api/categoriasDescrição: Cria uma nova categoria.Corpo da Requisição: CategoriaDTOResposta: 201 Created com CategoriaDTO e location header | 400 Bad RequestPUT /api/categorias/{id}Descrição: Atualiza uma categoria existente.Parâmetros de Rota:id (int): ID da categoria a ser atualizada.Corpo da Requisição: CategoriaDTO (o CategoriaId no corpo deve ser igual ao id da rota).Resposta: 200 OK com CategoriaDTO atualizada | 400 Bad RequestDELETE /api/categorias/{id}Descrição: Exclui uma categoria.Parâmetros de Rota:id (int): ID da categoria a ser excluída.Resposta: 200 OK com CategoriaDTO excluída | 404 Not FoundProdutos (/api/produtos) - (Estrutura Inferida)GET /api/produtosDescrição: Lista todos os produtos de forma paginada.Parâmetros de Query: (ex: PageNumber, PageSize, filtros específicos de produto).Headers de Resposta: X-Pagination com metadados da paginação.Resposta: 200 OK com IEnumerable<ProdutoDTO> | 404 Not FoundGET /api/produtos/{id}Descrição: Obtém um produto específico pelo seu ID.Parâmetros de Rota:id (int): ID do produto.Resposta: 200 OK com ProdutoDTO | 404 Not FoundPOST /api/produtosDescrição: Cria um novo produto.Corpo da Requisição: ProdutoDTOResposta: 201 Created com ProdutoDTO e location header | 400 Bad RequestPUT /api/produtos/{id}Descrição: Atualiza um produto existente.Parâmetros de Rota:id (int): ID do produto a ser atualizado.Corpo da Requisição: ProdutoDTO (o ProdutoId no corpo deve ser igual ao id da rota).Resposta: 200 OK com ProdutoDTO atualizado | 400 Bad RequestDELETE /api/produtos/{id}Descrição: Exclui um produto.Parâmetros de Rota:id (int): ID do produto a ser excluído.Resposta: 200 OK com ProdutoDTO excluído | 404 Not Found(Se houver um controller de autenticação, como por exemplo AuthController, adicione seus endpoints aqui também, ex: /api/auth/login, /api/auth/register)Tecnologias UtilizadasLinguagem Principal: C#Framework Principal: ASP.NET Core (especifique a versão, ex: 6.0)Banco de Dados: [Especifique qual SGBD, ex: PostgreSQL, MySQL, SQL Server] (rodando em Docker)ORM: Entity Framework Core (especifique a versão)Mapeamento: AutoMapperAutenticação: JWT (JSON Web Tokens)Outras bibliotecas ou ferramentas importantes:Logging (ex: Microsoft.Extensions.Logging)Testes (ex: xUnit, Moq - se utilizados)ContribuiçãoSe desejar contribuir,
